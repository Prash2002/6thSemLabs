#include <stdio.h>

void main()
{
    /* test */
    char a[50] = "Hi this a normal string";
    /*test
    two */
    int x, y;
    printf("A normal statement");
    int z, , a;
    int r = "ee";
    char b[100] = " this is a problem 
        fosho;
    ";
        if (
            (a * c) == (b + d)

        /*
    {
    }
    if ( a
    {
        return 0;
}
